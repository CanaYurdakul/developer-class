string brand = 'Toyota';
system.debug(brand);

string model = 'Venza';
system.debug(model);

string color = 'Grey';
system.debug(color);

string myCar = brand + ' ' + model + ' ' + color;
system.debug(myCar);