string firstVariable = 'Tjis is our first string variable';
system.debug(firstVariable);

string firstName = 'Cana';
string lastName = 'Yurdakul';
string fullName = firstName + ' ' + lastName;

System.debug(fullName);